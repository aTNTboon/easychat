<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 定义日志文件的存储路径（可替换为实际路径） -->
    <!-- 按要求调整为static目录相对路径 -->
    <property name="LOG_PATH" value="${LOG_PATH:-./src/main/resources/static/logs}" />
    <property name="LOG_FILE" value="${LOG_FILE:-${LOG_PATH}/application_%d{yyyy-MM-dd}.log}" />

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出：按日期和大小滚动 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 每天生成一个日志文件，压缩格式 -->
            <fileNamePattern>${LOG_PATH}/application.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <!-- 单个日志文件最大 10MB -->
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <!-- 保留 30 天的历史日志 -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 开发环境：控制台输出 DEBUG 及以上级别，不输出到文件 -->
    <springProfile name="dev">
        <root level="DEBUG">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>

    <!-- 生产环境：文件输出 INFO 及以上级别，控制台输出 WARN 及以上（可选） -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="FILE" />
            <appender-ref ref="CONSOLE" /> <!-- 生产环境若需要控制台可保留，否则删除此行 -->
        </root>
        <!-- 针对特定包设置更低的日志级别，例如调试SQL -->
        <logger name="com.jiawa" level="DEBUG" additivity="false">
            <appender-ref ref="FILE" />
        </logger>
    </springProfile>

    <!-- 默认环境（无 profile 时）：控制台输出 INFO 及以上 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>